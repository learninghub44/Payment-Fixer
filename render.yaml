services:
  # Backend API Service
  - type: web
    name: kuwesa-api
    runtime: node
    plan: free
    buildCommand: pnpm --filter @workspace/api-server install && pnpm --filter @workspace/api-server run build
    startCommand: pnpm --filter @workspace/api-server run start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /api/healthz

  # Frontend Service
  - type: web
    name: kuwesa-frontend
    runtime: static
    plan: free
    buildCommand: pnpm --filter @workspace/relationships install && pnpm --filter @workspace/relationships run build
    staticPublishPath: artifacts/relationships/dist/public
    envVars:
      - key: VITE_API_URL
        value: https://kuwesa-api.onrender.com/api
    routes:
      - type: rewrite
        source: /api/*
        destination: https://kuwesa-api.onrender.com/api/*
      - type: file
        source: /**
        destination: /index.html
